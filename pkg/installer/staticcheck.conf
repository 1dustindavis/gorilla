# The package still contains two legacy download URL globals that are not read.
# Keep all inherited Staticcheck checks enabled while exempting only U1000 in
# this package until those declarations can be removed in a focused cleanup.
checks = ["inherit", "-U1000"]
